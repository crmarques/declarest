apiVersion: v1
kind: Service
metadata:
  name: secret-provider-vault
  labels:
    app.kubernetes.io/name: secret-provider-vault
    declarest.e2e/component-key: secret-provider-vault
  annotations:
    declarest.e2e/port-forward: "${VAULT_PORT}:8200"
spec:
  selector:
    app.kubernetes.io/name: secret-provider-vault
  ports:
    - name: http
      port: 8200
      targetPort: 8200
