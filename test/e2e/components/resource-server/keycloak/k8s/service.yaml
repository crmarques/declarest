apiVersion: v1
kind: Service
metadata:
  name: resource-server-keycloak
  labels:
    app.kubernetes.io/name: resource-server-keycloak
    declarest.e2e/component-key: resource-server-keycloak
  annotations:
    declarest.e2e/port-forward: "${KEYCLOAK_HOST_PORT}:8080"
spec:
  selector:
    app.kubernetes.io/name: resource-server-keycloak
  ports:
    - name: http
      port: 8080
      targetPort: 8080
