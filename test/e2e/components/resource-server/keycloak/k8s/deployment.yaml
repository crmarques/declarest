apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-server-keycloak
  labels:
    app.kubernetes.io/name: resource-server-keycloak
    declarest.e2e/component-key: resource-server-keycloak
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: resource-server-keycloak
  template:
    metadata:
      labels:
        app.kubernetes.io/name: resource-server-keycloak
        declarest.e2e/component-key: resource-server-keycloak
    spec:
      containers:
        - name: keycloak
          image: quay.io/keycloak/keycloak:26.2.5
          args:
            - start-dev
            - --http-port=8080
          env:
            - name: KEYCLOAK_ADMIN
              value: "${KEYCLOAK_ADMIN_USER}"
            - name: KEYCLOAK_ADMIN_PASSWORD
              value: "${KEYCLOAK_ADMIN_PASSWORD}"
          ports:
            - name: http
              containerPort: 8080
