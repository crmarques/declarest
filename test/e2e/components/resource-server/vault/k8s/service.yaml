apiVersion: v1
kind: Service
metadata:
  name: resource-server-vault
  labels:
    app.kubernetes.io/name: resource-server-vault
    declarest.e2e/component-key: resource-server-vault
  annotations:
    declarest.e2e/port-forward: "${VAULT_PORT}:8200"
spec:
  selector:
    app.kubernetes.io/name: resource-server-vault
  ports:
    - name: http
      port: 8200
      targetPort: 8200
