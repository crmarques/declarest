apiVersion: apps/v1
kind: Deployment
metadata:
  name: managed-server-vault
  labels:
    app.kubernetes.io/name: managed-server-vault
    declarest.e2e/component-key: managed-server-vault
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: managed-server-vault
  template:
    metadata:
      labels:
        app.kubernetes.io/name: managed-server-vault
        declarest.e2e/component-key: managed-server-vault
    spec:
      containers:
        - name: vault
          image: docker.io/hashicorp/vault:1.21
          securityContext:
            capabilities:
              add:
                - IPC_LOCK
          env:
            - name: VAULT_DEV_ROOT_TOKEN_ID
              value: "${VAULT_TOKEN}"
            - name: VAULT_DEV_LISTEN_ADDRESS
              value: 0.0.0.0:8200
          ports:
            - name: http
              containerPort: 8200
