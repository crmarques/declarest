apiVersion: v1
kind: Service
metadata:
  name: managed-server-rundeck
  labels:
    app.kubernetes.io/name: managed-server-rundeck
    declarest.e2e/component-key: managed-server-rundeck
  annotations:
    declarest.e2e/port-forward: "${RUNDECK_HTTP_PORT}:4440"
spec:
  selector:
    app.kubernetes.io/name: managed-server-rundeck
  ports:
    - name: http
      port: 4440
      targetPort: 4440
