apiVersion: v1
kind: Service
metadata:
  name: managed-server-keycloak
  labels:
    app.kubernetes.io/name: managed-server-keycloak
    declarest.e2e/component-key: managed-server-keycloak
  annotations:
    declarest.e2e/port-forward: "${KEYCLOAK_HOST_PORT}:8080"
spec:
  selector:
    app.kubernetes.io/name: managed-server-keycloak
  ports:
    - name: http
      port: 8080
      targetPort: 8080
