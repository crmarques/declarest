apiVersion: declarest.io/v1alpha1
kind: ResourceRepository
metadata:
  name: example-repository
spec:
  # Context parity:
  # - repository.git.remote.*   -> spec.git.*
  type: git
  pollInterval: 1m
  git:
    url: https://github.com/example/declarest-resources.git
    branch: main
    auth:
      tokenRef:
        name: repository-credentials
        key: token
    webhook:
      provider: gitea
      secretRef:
        name: repository-credentials
        key: webhook-secret
  storage:
    pvc:
      accessModes:
        - ReadWriteOnce
      requests:
        storage: 1Gi
