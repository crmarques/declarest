# Secret Providers

Secret provider-specific helpers should live here (file, vault).
Managed server harnesses can source these scripts for consistent setup.
Each provider must expose a `component.sh` contract; see `docs/e2e-components.md`.
