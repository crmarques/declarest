# <Domain Title>

## Purpose
One sentence: what this file owns.

## Normative Rules
1. `MUST` for non-negotiable behavior, `SHOULD` for strong recommendations, `MAY` for options.
2. Each rule is observable and testable (explicit condition → outcome).
3. For concepts owned by another file, write a one-line pointer instead of restating the rule.

## Data Contracts
Include only where it adds beyond `agents/reference/interfaces.md`; otherwise reference interfaces.md for canonical names.

## Failure Modes
Predictable failure classes with their error category (per `agents/reference/interfaces.md`).

## Examples
At least one normal scenario and one corner case.
