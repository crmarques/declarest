# Installation

CLI installation now lives in [Quickstart (CLI)](quickstart.md) so setup and first use stay in one guided flow.

Use that page if you are starting from scratch or want the current install commands.
